name: radarr

services:
  radarr:
    image: linuxserver/radarr
    container_name: radarr
    networks:
      - lab
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
    volumes:
      - ../data:/config
      - /mnt/docker-data/media/movies:/movies
      - /mnt/docker-scratch/downloads:/downloads
    ports:
      - '38083:7878'
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.movies.rule=Host(`movies.thegarden`)
      - traefik.http.routers.movies.entrypoints=websecure
      - traefik.http.routers.movies.tls=true
      - traefik.http.services.movies.loadbalancer.server.port=7878

networks:
  lab:
    external: true
