IMMICH_VERSION=v3
TZ=America/New_York

# Postgres auth. Characters A-Za-z0-9 only, no special characters or spaces
# (Immich's own requirement).
DB_PASSWORD=changeme
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
